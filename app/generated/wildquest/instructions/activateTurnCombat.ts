/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findGameConfigPda } from "../pdas";
import { WILDQUEST_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const ACTIVATE_TURN_COMBAT_DISCRIMINATOR = new Uint8Array([
  30, 160, 174, 201, 183, 55, 171, 132,
]);

export function getActivateTurnCombatDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACTIVATE_TURN_COMBAT_DISCRIMINATOR,
  );
}

export type ActivateTurnCombatInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountGameConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountGameConfig extends string
        ? WritableAccount<TAccountGameConfig>
        : TAccountGameConfig,
      ...TRemainingAccounts,
    ]
  >;

export type ActivateTurnCombatInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ActivateTurnCombatInstructionDataArgs = {};

export function getActivateTurnCombatInstructionDataEncoder(): FixedSizeEncoder<ActivateTurnCombatInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ACTIVATE_TURN_COMBAT_DISCRIMINATOR,
    }),
  );
}

export function getActivateTurnCombatInstructionDataDecoder(): FixedSizeDecoder<ActivateTurnCombatInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getActivateTurnCombatInstructionDataCodec(): FixedSizeCodec<
  ActivateTurnCombatInstructionDataArgs,
  ActivateTurnCombatInstructionData
> {
  return combineCodec(
    getActivateTurnCombatInstructionDataEncoder(),
    getActivateTurnCombatInstructionDataDecoder(),
  );
}

export type ActivateTurnCombatAsyncInput<
  TAccountAdmin extends string = string,
  TAccountGameConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  gameConfig?: Address<TAccountGameConfig>;
};

export async function getActivateTurnCombatInstructionAsync<
  TAccountAdmin extends string,
  TAccountGameConfig extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: ActivateTurnCombatAsyncInput<TAccountAdmin, TAccountGameConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ActivateTurnCombatInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    gameConfig: { value: input.gameConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.gameConfig.value) {
    accounts.gameConfig.value = await findGameConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.gameConfig),
    ],
    data: getActivateTurnCombatInstructionDataEncoder().encode({}),
    programAddress,
  } as ActivateTurnCombatInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig
  >);
}

export type ActivateTurnCombatInput<
  TAccountAdmin extends string = string,
  TAccountGameConfig extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  gameConfig: Address<TAccountGameConfig>;
};

export function getActivateTurnCombatInstruction<
  TAccountAdmin extends string,
  TAccountGameConfig extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: ActivateTurnCombatInput<TAccountAdmin, TAccountGameConfig>,
  config?: { programAddress?: TProgramAddress },
): ActivateTurnCombatInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountGameConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    gameConfig: { value: input.gameConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.gameConfig),
    ],
    data: getActivateTurnCombatInstructionDataEncoder().encode({}),
    programAddress,
  } as ActivateTurnCombatInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig
  >);
}

export type ParsedActivateTurnCombatInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    gameConfig: TAccountMetas[1];
  };
  data: ActivateTurnCombatInstructionData;
};

export function parseActivateTurnCombatInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedActivateTurnCombatInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), gameConfig: getNextAccount() },
    data: getActivateTurnCombatInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
