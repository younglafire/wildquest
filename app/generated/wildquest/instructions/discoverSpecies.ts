/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findDiscoveryPda, findPlayerPda } from "../pdas";
import { WILDQUEST_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const DISCOVER_SPECIES_DISCRIMINATOR = new Uint8Array([
  200, 114, 104, 62, 123, 143, 65, 66,
]);

export function getDiscoverSpeciesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISCOVER_SPECIES_DISCRIMINATOR,
  );
}

export type DiscoverSpeciesInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountPlayer extends string | AccountMeta<string> = string,
  TAccountDiscovery extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountPlayer extends string
        ? WritableAccount<TAccountPlayer>
        : TAccountPlayer,
      TAccountDiscovery extends string
        ? WritableAccount<TAccountDiscovery>
        : TAccountDiscovery,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DiscoverSpeciesInstructionData = {
  discriminator: ReadonlyUint8Array;
  speciesId: bigint;
  grade: number;
  rarity: number;
  proofHash: ReadonlyUint8Array;
};

export type DiscoverSpeciesInstructionDataArgs = {
  speciesId: number | bigint;
  grade: number;
  rarity: number;
  proofHash: ReadonlyUint8Array;
};

export function getDiscoverSpeciesInstructionDataEncoder(): FixedSizeEncoder<DiscoverSpeciesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["speciesId", getU64Encoder()],
      ["grade", getU8Encoder()],
      ["rarity", getU8Encoder()],
      ["proofHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: DISCOVER_SPECIES_DISCRIMINATOR }),
  );
}

export function getDiscoverSpeciesInstructionDataDecoder(): FixedSizeDecoder<DiscoverSpeciesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["speciesId", getU64Decoder()],
    ["grade", getU8Decoder()],
    ["rarity", getU8Decoder()],
    ["proofHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getDiscoverSpeciesInstructionDataCodec(): FixedSizeCodec<
  DiscoverSpeciesInstructionDataArgs,
  DiscoverSpeciesInstructionData
> {
  return combineCodec(
    getDiscoverSpeciesInstructionDataEncoder(),
    getDiscoverSpeciesInstructionDataDecoder(),
  );
}

export type DiscoverSpeciesAsyncInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountDiscovery extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  player?: Address<TAccountPlayer>;
  discovery?: Address<TAccountDiscovery>;
  systemProgram?: Address<TAccountSystemProgram>;
  speciesId: DiscoverSpeciesInstructionDataArgs["speciesId"];
  grade: DiscoverSpeciesInstructionDataArgs["grade"];
  rarity: DiscoverSpeciesInstructionDataArgs["rarity"];
  proofHash: DiscoverSpeciesInstructionDataArgs["proofHash"];
};

export async function getDiscoverSpeciesInstructionAsync<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountDiscovery extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: DiscoverSpeciesAsyncInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountDiscovery,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DiscoverSpeciesInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountDiscovery,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    discovery: { value: input.discovery ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.player.value) {
    accounts.player.value = await findPlayerPda({
      payer: expectAddress(accounts.payer.value),
    });
  }
  if (!accounts.discovery.value) {
    accounts.discovery.value = await findDiscoveryPda({
      payer: expectAddress(accounts.payer.value),
      speciesId: expectSome(args.speciesId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.discovery),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDiscoverSpeciesInstructionDataEncoder().encode(
      args as DiscoverSpeciesInstructionDataArgs,
    ),
    programAddress,
  } as DiscoverSpeciesInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountDiscovery,
    TAccountSystemProgram
  >);
}

export type DiscoverSpeciesInput<
  TAccountPayer extends string = string,
  TAccountPlayer extends string = string,
  TAccountDiscovery extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  player: Address<TAccountPlayer>;
  discovery: Address<TAccountDiscovery>;
  systemProgram?: Address<TAccountSystemProgram>;
  speciesId: DiscoverSpeciesInstructionDataArgs["speciesId"];
  grade: DiscoverSpeciesInstructionDataArgs["grade"];
  rarity: DiscoverSpeciesInstructionDataArgs["rarity"];
  proofHash: DiscoverSpeciesInstructionDataArgs["proofHash"];
};

export function getDiscoverSpeciesInstruction<
  TAccountPayer extends string,
  TAccountPlayer extends string,
  TAccountDiscovery extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: DiscoverSpeciesInput<
    TAccountPayer,
    TAccountPlayer,
    TAccountDiscovery,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DiscoverSpeciesInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountPlayer,
  TAccountDiscovery,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    player: { value: input.player ?? null, isWritable: true },
    discovery: { value: input.discovery ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.discovery),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDiscoverSpeciesInstructionDataEncoder().encode(
      args as DiscoverSpeciesInstructionDataArgs,
    ),
    programAddress,
  } as DiscoverSpeciesInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountPlayer,
    TAccountDiscovery,
    TAccountSystemProgram
  >);
}

export type ParsedDiscoverSpeciesInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    player: TAccountMetas[1];
    discovery: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: DiscoverSpeciesInstructionData;
};

export function parseDiscoverSpeciesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDiscoverSpeciesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      player: getNextAccount(),
      discovery: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDiscoverSpeciesInstructionDataDecoder().decode(instruction.data),
  };
}
