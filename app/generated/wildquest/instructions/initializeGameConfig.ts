/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findGameConfigPda } from "../pdas";
import { WILDQUEST_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_GAME_CONFIG_DISCRIMINATOR = new Uint8Array([
  45, 61, 80, 55, 152, 63, 158, 47,
]);

export function getInitializeGameConfigDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_GAME_CONFIG_DISCRIMINATOR,
  );
}

export type InitializeGameConfigInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountGameConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountGameConfig extends string
        ? WritableAccount<TAccountGameConfig>
        : TAccountGameConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeGameConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  captureAuthority: Address;
};

export type InitializeGameConfigInstructionDataArgs = {
  captureAuthority: Address;
};

export function getInitializeGameConfigInstructionDataEncoder(): FixedSizeEncoder<InitializeGameConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["captureAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_GAME_CONFIG_DISCRIMINATOR,
    }),
  );
}

export function getInitializeGameConfigInstructionDataDecoder(): FixedSizeDecoder<InitializeGameConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["captureAuthority", getAddressDecoder()],
  ]);
}

export function getInitializeGameConfigInstructionDataCodec(): FixedSizeCodec<
  InitializeGameConfigInstructionDataArgs,
  InitializeGameConfigInstructionData
> {
  return combineCodec(
    getInitializeGameConfigInstructionDataEncoder(),
    getInitializeGameConfigInstructionDataDecoder(),
  );
}

export type InitializeGameConfigAsyncInput<
  TAccountAdmin extends string = string,
  TAccountGameConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  gameConfig?: Address<TAccountGameConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  captureAuthority: InitializeGameConfigInstructionDataArgs["captureAuthority"];
};

export async function getInitializeGameConfigInstructionAsync<
  TAccountAdmin extends string,
  TAccountGameConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: InitializeGameConfigAsyncInput<
    TAccountAdmin,
    TAccountGameConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeGameConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    gameConfig: { value: input.gameConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.gameConfig.value) {
    accounts.gameConfig.value = await findGameConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeGameConfigInstructionDataEncoder().encode(
      args as InitializeGameConfigInstructionDataArgs,
    ),
    programAddress,
  } as InitializeGameConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig,
    TAccountSystemProgram
  >);
}

export type InitializeGameConfigInput<
  TAccountAdmin extends string = string,
  TAccountGameConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  gameConfig: Address<TAccountGameConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  captureAuthority: InitializeGameConfigInstructionDataArgs["captureAuthority"];
};

export function getInitializeGameConfigInstruction<
  TAccountAdmin extends string,
  TAccountGameConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: InitializeGameConfigInput<
    TAccountAdmin,
    TAccountGameConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeGameConfigInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountGameConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    gameConfig: { value: input.gameConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeGameConfigInstructionDataEncoder().encode(
      args as InitializeGameConfigInstructionDataArgs,
    ),
    programAddress,
  } as InitializeGameConfigInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountGameConfig,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeGameConfigInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    gameConfig: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeGameConfigInstructionData;
};

export function parseInitializeGameConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeGameConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      gameConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeGameConfigInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
