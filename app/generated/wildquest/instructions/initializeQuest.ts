/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findQuestPda } from "../pdas";
import { WILDQUEST_PROGRAM_ADDRESS } from "../programs";
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const INITIALIZE_QUEST_DISCRIMINATOR = new Uint8Array([
  180, 23, 74, 210, 141, 45, 251, 99,
]);

export function getInitializeQuestDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_QUEST_DISCRIMINATOR,
  );
}

export type InitializeQuestInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountQuest extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountQuest extends string
        ? WritableAccount<TAccountQuest>
        : TAccountQuest,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeQuestInstructionData = {
  discriminator: ReadonlyUint8Array;
  questId: bigint;
};

export type InitializeQuestInstructionDataArgs = { questId: number | bigint };

export function getInitializeQuestInstructionDataEncoder(): FixedSizeEncoder<InitializeQuestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["questId", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_QUEST_DISCRIMINATOR }),
  );
}

export function getInitializeQuestInstructionDataDecoder(): FixedSizeDecoder<InitializeQuestInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["questId", getU64Decoder()],
  ]);
}

export function getInitializeQuestInstructionDataCodec(): FixedSizeCodec<
  InitializeQuestInstructionDataArgs,
  InitializeQuestInstructionData
> {
  return combineCodec(
    getInitializeQuestInstructionDataEncoder(),
    getInitializeQuestInstructionDataDecoder(),
  );
}

export type InitializeQuestAsyncInput<
  TAccountPayer extends string = string,
  TAccountQuest extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  quest?: Address<TAccountQuest>;
  systemProgram?: Address<TAccountSystemProgram>;
  questId: InitializeQuestInstructionDataArgs["questId"];
};

export async function getInitializeQuestInstructionAsync<
  TAccountPayer extends string,
  TAccountQuest extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: InitializeQuestAsyncInput<
    TAccountPayer,
    TAccountQuest,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeQuestInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountQuest,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    quest: { value: input.quest ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.quest.value) {
    accounts.quest.value = await findQuestPda({
      questId: expectSome(args.questId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.quest),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeQuestInstructionDataEncoder().encode(
      args as InitializeQuestInstructionDataArgs,
    ),
    programAddress,
  } as InitializeQuestInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountQuest,
    TAccountSystemProgram
  >);
}

export type InitializeQuestInput<
  TAccountPayer extends string = string,
  TAccountQuest extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  quest: Address<TAccountQuest>;
  systemProgram?: Address<TAccountSystemProgram>;
  questId: InitializeQuestInstructionDataArgs["questId"];
};

export function getInitializeQuestInstruction<
  TAccountPayer extends string,
  TAccountQuest extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: InitializeQuestInput<
    TAccountPayer,
    TAccountQuest,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeQuestInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountQuest,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    quest: { value: input.quest ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.quest),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeQuestInstructionDataEncoder().encode(
      args as InitializeQuestInstructionDataArgs,
    ),
    programAddress,
  } as InitializeQuestInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountQuest,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeQuestInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    quest: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeQuestInstructionData;
};

export function parseInitializeQuestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeQuestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      quest: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeQuestInstructionDataDecoder().decode(instruction.data),
  };
}
