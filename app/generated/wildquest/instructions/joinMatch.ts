/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findGameConfigPda } from "../pdas";
import { WILDQUEST_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const JOIN_MATCH_DISCRIMINATOR = new Uint8Array([
  244, 8, 47, 130, 192, 59, 179, 44,
]);

export function getJoinMatchDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(JOIN_MATCH_DISCRIMINATOR);
}

export type JoinMatchInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountOpponent extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountGameConfig extends string | AccountMeta<string> = string,
  TAccountMatchAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOpponent extends string
        ? WritableSignerAccount<TAccountOpponent> &
            AccountSignerMeta<TAccountOpponent>
        : TAccountOpponent,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountGameConfig extends string
        ? ReadonlyAccount<TAccountGameConfig>
        : TAccountGameConfig,
      TAccountMatchAccount extends string
        ? WritableAccount<TAccountMatchAccount>
        : TAccountMatchAccount,
      ...TRemainingAccounts,
    ]
  >;

export type JoinMatchInstructionData = { discriminator: ReadonlyUint8Array };

export type JoinMatchInstructionDataArgs = {};

export function getJoinMatchInstructionDataEncoder(): FixedSizeEncoder<JoinMatchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: JOIN_MATCH_DISCRIMINATOR }),
  );
}

export function getJoinMatchInstructionDataDecoder(): FixedSizeDecoder<JoinMatchInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getJoinMatchInstructionDataCodec(): FixedSizeCodec<
  JoinMatchInstructionDataArgs,
  JoinMatchInstructionData
> {
  return combineCodec(
    getJoinMatchInstructionDataEncoder(),
    getJoinMatchInstructionDataDecoder(),
  );
}

export type JoinMatchAsyncInput<
  TAccountOpponent extends string = string,
  TAccountCreator extends string = string,
  TAccountGameConfig extends string = string,
  TAccountMatchAccount extends string = string,
> = {
  opponent: TransactionSigner<TAccountOpponent>;
  creator: Address<TAccountCreator>;
  gameConfig?: Address<TAccountGameConfig>;
  matchAccount: Address<TAccountMatchAccount>;
};

export async function getJoinMatchInstructionAsync<
  TAccountOpponent extends string,
  TAccountCreator extends string,
  TAccountGameConfig extends string,
  TAccountMatchAccount extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: JoinMatchAsyncInput<
    TAccountOpponent,
    TAccountCreator,
    TAccountGameConfig,
    TAccountMatchAccount
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  JoinMatchInstruction<
    TProgramAddress,
    TAccountOpponent,
    TAccountCreator,
    TAccountGameConfig,
    TAccountMatchAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    opponent: { value: input.opponent ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    gameConfig: { value: input.gameConfig ?? null, isWritable: false },
    matchAccount: { value: input.matchAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.gameConfig.value) {
    accounts.gameConfig.value = await findGameConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.opponent),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.matchAccount),
    ],
    data: getJoinMatchInstructionDataEncoder().encode({}),
    programAddress,
  } as JoinMatchInstruction<
    TProgramAddress,
    TAccountOpponent,
    TAccountCreator,
    TAccountGameConfig,
    TAccountMatchAccount
  >);
}

export type JoinMatchInput<
  TAccountOpponent extends string = string,
  TAccountCreator extends string = string,
  TAccountGameConfig extends string = string,
  TAccountMatchAccount extends string = string,
> = {
  opponent: TransactionSigner<TAccountOpponent>;
  creator: Address<TAccountCreator>;
  gameConfig: Address<TAccountGameConfig>;
  matchAccount: Address<TAccountMatchAccount>;
};

export function getJoinMatchInstruction<
  TAccountOpponent extends string,
  TAccountCreator extends string,
  TAccountGameConfig extends string,
  TAccountMatchAccount extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: JoinMatchInput<
    TAccountOpponent,
    TAccountCreator,
    TAccountGameConfig,
    TAccountMatchAccount
  >,
  config?: { programAddress?: TProgramAddress },
): JoinMatchInstruction<
  TProgramAddress,
  TAccountOpponent,
  TAccountCreator,
  TAccountGameConfig,
  TAccountMatchAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    opponent: { value: input.opponent ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    gameConfig: { value: input.gameConfig ?? null, isWritable: false },
    matchAccount: { value: input.matchAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.opponent),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.matchAccount),
    ],
    data: getJoinMatchInstructionDataEncoder().encode({}),
    programAddress,
  } as JoinMatchInstruction<
    TProgramAddress,
    TAccountOpponent,
    TAccountCreator,
    TAccountGameConfig,
    TAccountMatchAccount
  >);
}

export type ParsedJoinMatchInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    opponent: TAccountMetas[0];
    creator: TAccountMetas[1];
    gameConfig: TAccountMetas[2];
    matchAccount: TAccountMetas[3];
  };
  data: JoinMatchInstructionData;
};

export function parseJoinMatchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedJoinMatchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      opponent: getNextAccount(),
      creator: getNextAccount(),
      gameConfig: getNextAccount(),
      matchAccount: getNextAccount(),
    },
    data: getJoinMatchInstructionDataDecoder().decode(instruction.data),
  };
}
