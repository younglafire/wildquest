/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { WILDQUEST_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REFUND_STALE_MATCH_DISCRIMINATOR = new Uint8Array([
  122, 68, 199, 156, 243, 159, 29, 50,
]);

export function getRefundStaleMatchDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REFUND_STALE_MATCH_DISCRIMINATOR,
  );
}

export type RefundStaleMatchInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountParticipant extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountOpponent extends string | AccountMeta<string> = string,
  TAccountMatchAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountParticipant extends string
        ? ReadonlySignerAccount<TAccountParticipant> &
            AccountSignerMeta<TAccountParticipant>
        : TAccountParticipant,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountOpponent extends string
        ? WritableAccount<TAccountOpponent>
        : TAccountOpponent,
      TAccountMatchAccount extends string
        ? WritableAccount<TAccountMatchAccount>
        : TAccountMatchAccount,
      ...TRemainingAccounts,
    ]
  >;

export type RefundStaleMatchInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RefundStaleMatchInstructionDataArgs = {};

export function getRefundStaleMatchInstructionDataEncoder(): FixedSizeEncoder<RefundStaleMatchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REFUND_STALE_MATCH_DISCRIMINATOR }),
  );
}

export function getRefundStaleMatchInstructionDataDecoder(): FixedSizeDecoder<RefundStaleMatchInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundStaleMatchInstructionDataCodec(): FixedSizeCodec<
  RefundStaleMatchInstructionDataArgs,
  RefundStaleMatchInstructionData
> {
  return combineCodec(
    getRefundStaleMatchInstructionDataEncoder(),
    getRefundStaleMatchInstructionDataDecoder(),
  );
}

export type RefundStaleMatchInput<
  TAccountParticipant extends string = string,
  TAccountCreator extends string = string,
  TAccountOpponent extends string = string,
  TAccountMatchAccount extends string = string,
> = {
  participant: TransactionSigner<TAccountParticipant>;
  creator: Address<TAccountCreator>;
  opponent: Address<TAccountOpponent>;
  matchAccount: Address<TAccountMatchAccount>;
};

export function getRefundStaleMatchInstruction<
  TAccountParticipant extends string,
  TAccountCreator extends string,
  TAccountOpponent extends string,
  TAccountMatchAccount extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: RefundStaleMatchInput<
    TAccountParticipant,
    TAccountCreator,
    TAccountOpponent,
    TAccountMatchAccount
  >,
  config?: { programAddress?: TProgramAddress },
): RefundStaleMatchInstruction<
  TProgramAddress,
  TAccountParticipant,
  TAccountCreator,
  TAccountOpponent,
  TAccountMatchAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    participant: { value: input.participant ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    opponent: { value: input.opponent ?? null, isWritable: true },
    matchAccount: { value: input.matchAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.participant),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.opponent),
      getAccountMeta(accounts.matchAccount),
    ],
    data: getRefundStaleMatchInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundStaleMatchInstruction<
    TProgramAddress,
    TAccountParticipant,
    TAccountCreator,
    TAccountOpponent,
    TAccountMatchAccount
  >);
}

export type ParsedRefundStaleMatchInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    participant: TAccountMetas[0];
    creator: TAccountMetas[1];
    opponent: TAccountMetas[2];
    matchAccount: TAccountMetas[3];
  };
  data: RefundStaleMatchInstructionData;
};

export function parseRefundStaleMatchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRefundStaleMatchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      participant: getNextAccount(),
      creator: getNextAccount(),
      opponent: getNextAccount(),
      matchAccount: getNextAccount(),
    },
    data: getRefundStaleMatchInstructionDataDecoder().decode(instruction.data),
  };
}
