/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { WILDQUEST_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const RELEASE_CREATURE_DISCRIMINATOR = new Uint8Array([
  5, 94, 180, 100, 108, 136, 84, 214,
]);

export function getReleaseCreatureDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RELEASE_CREATURE_DISCRIMINATOR,
  );
}

export type ReleaseCreatureInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountCreature extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountCreature extends string
        ? WritableAccount<TAccountCreature>
        : TAccountCreature,
      ...TRemainingAccounts,
    ]
  >;

export type ReleaseCreatureInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReleaseCreatureInstructionDataArgs = {};

export function getReleaseCreatureInstructionDataEncoder(): FixedSizeEncoder<ReleaseCreatureInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RELEASE_CREATURE_DISCRIMINATOR }),
  );
}

export function getReleaseCreatureInstructionDataDecoder(): FixedSizeDecoder<ReleaseCreatureInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReleaseCreatureInstructionDataCodec(): FixedSizeCodec<
  ReleaseCreatureInstructionDataArgs,
  ReleaseCreatureInstructionData
> {
  return combineCodec(
    getReleaseCreatureInstructionDataEncoder(),
    getReleaseCreatureInstructionDataDecoder(),
  );
}

export type ReleaseCreatureInput<
  TAccountOwner extends string = string,
  TAccountCreature extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  creature: Address<TAccountCreature>;
};

export function getReleaseCreatureInstruction<
  TAccountOwner extends string,
  TAccountCreature extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: ReleaseCreatureInput<TAccountOwner, TAccountCreature>,
  config?: { programAddress?: TProgramAddress },
): ReleaseCreatureInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountCreature
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    creature: { value: input.creature ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.creature),
    ],
    data: getReleaseCreatureInstructionDataEncoder().encode({}),
    programAddress,
  } as ReleaseCreatureInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCreature
  >);
}

export type ParsedReleaseCreatureInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    creature: TAccountMetas[1];
  };
  data: ReleaseCreatureInstructionData;
};

export function parseReleaseCreatureInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReleaseCreatureInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), creature: getNextAccount() },
    data: getReleaseCreatureInstructionDataDecoder().decode(instruction.data),
  };
}
