/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findGameConfigPda } from "../pdas";
import { WILDQUEST_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const RESOLVE_MATCH_DISCRIMINATOR = new Uint8Array([
  73, 0, 15, 197, 178, 47, 21, 193,
]);

export function getResolveMatchDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESOLVE_MATCH_DISCRIMINATOR,
  );
}

export type ResolveMatchInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountResolver extends string | AccountMeta<string> = string,
  TAccountGameConfig extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountOpponent extends string | AccountMeta<string> = string,
  TAccountMatchAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountResolver extends string
        ? ReadonlySignerAccount<TAccountResolver> &
            AccountSignerMeta<TAccountResolver>
        : TAccountResolver,
      TAccountGameConfig extends string
        ? ReadonlyAccount<TAccountGameConfig>
        : TAccountGameConfig,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountOpponent extends string
        ? WritableAccount<TAccountOpponent>
        : TAccountOpponent,
      TAccountMatchAccount extends string
        ? WritableAccount<TAccountMatchAccount>
        : TAccountMatchAccount,
      ...TRemainingAccounts,
    ]
  >;

export type ResolveMatchInstructionData = {
  discriminator: ReadonlyUint8Array;
  winner: Option<Address>;
  turnCount: number;
  resultHash: ReadonlyUint8Array;
};

export type ResolveMatchInstructionDataArgs = {
  winner: OptionOrNullable<Address>;
  turnCount: number;
  resultHash: ReadonlyUint8Array;
};

export function getResolveMatchInstructionDataEncoder(): Encoder<ResolveMatchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["winner", getOptionEncoder(getAddressEncoder())],
      ["turnCount", getU16Encoder()],
      ["resultHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: RESOLVE_MATCH_DISCRIMINATOR }),
  );
}

export function getResolveMatchInstructionDataDecoder(): Decoder<ResolveMatchInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["winner", getOptionDecoder(getAddressDecoder())],
    ["turnCount", getU16Decoder()],
    ["resultHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getResolveMatchInstructionDataCodec(): Codec<
  ResolveMatchInstructionDataArgs,
  ResolveMatchInstructionData
> {
  return combineCodec(
    getResolveMatchInstructionDataEncoder(),
    getResolveMatchInstructionDataDecoder(),
  );
}

export type ResolveMatchAsyncInput<
  TAccountResolver extends string = string,
  TAccountGameConfig extends string = string,
  TAccountCreator extends string = string,
  TAccountOpponent extends string = string,
  TAccountMatchAccount extends string = string,
> = {
  resolver: TransactionSigner<TAccountResolver>;
  gameConfig?: Address<TAccountGameConfig>;
  creator: Address<TAccountCreator>;
  opponent: Address<TAccountOpponent>;
  matchAccount: Address<TAccountMatchAccount>;
  winner: ResolveMatchInstructionDataArgs["winner"];
  turnCount: ResolveMatchInstructionDataArgs["turnCount"];
  resultHash: ResolveMatchInstructionDataArgs["resultHash"];
};

export async function getResolveMatchInstructionAsync<
  TAccountResolver extends string,
  TAccountGameConfig extends string,
  TAccountCreator extends string,
  TAccountOpponent extends string,
  TAccountMatchAccount extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: ResolveMatchAsyncInput<
    TAccountResolver,
    TAccountGameConfig,
    TAccountCreator,
    TAccountOpponent,
    TAccountMatchAccount
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ResolveMatchInstruction<
    TProgramAddress,
    TAccountResolver,
    TAccountGameConfig,
    TAccountCreator,
    TAccountOpponent,
    TAccountMatchAccount
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    resolver: { value: input.resolver ?? null, isWritable: false },
    gameConfig: { value: input.gameConfig ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    opponent: { value: input.opponent ?? null, isWritable: true },
    matchAccount: { value: input.matchAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.gameConfig.value) {
    accounts.gameConfig.value = await findGameConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.resolver),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.opponent),
      getAccountMeta(accounts.matchAccount),
    ],
    data: getResolveMatchInstructionDataEncoder().encode(
      args as ResolveMatchInstructionDataArgs,
    ),
    programAddress,
  } as ResolveMatchInstruction<
    TProgramAddress,
    TAccountResolver,
    TAccountGameConfig,
    TAccountCreator,
    TAccountOpponent,
    TAccountMatchAccount
  >);
}

export type ResolveMatchInput<
  TAccountResolver extends string = string,
  TAccountGameConfig extends string = string,
  TAccountCreator extends string = string,
  TAccountOpponent extends string = string,
  TAccountMatchAccount extends string = string,
> = {
  resolver: TransactionSigner<TAccountResolver>;
  gameConfig: Address<TAccountGameConfig>;
  creator: Address<TAccountCreator>;
  opponent: Address<TAccountOpponent>;
  matchAccount: Address<TAccountMatchAccount>;
  winner: ResolveMatchInstructionDataArgs["winner"];
  turnCount: ResolveMatchInstructionDataArgs["turnCount"];
  resultHash: ResolveMatchInstructionDataArgs["resultHash"];
};

export function getResolveMatchInstruction<
  TAccountResolver extends string,
  TAccountGameConfig extends string,
  TAccountCreator extends string,
  TAccountOpponent extends string,
  TAccountMatchAccount extends string,
  TProgramAddress extends Address = typeof WILDQUEST_PROGRAM_ADDRESS,
>(
  input: ResolveMatchInput<
    TAccountResolver,
    TAccountGameConfig,
    TAccountCreator,
    TAccountOpponent,
    TAccountMatchAccount
  >,
  config?: { programAddress?: TProgramAddress },
): ResolveMatchInstruction<
  TProgramAddress,
  TAccountResolver,
  TAccountGameConfig,
  TAccountCreator,
  TAccountOpponent,
  TAccountMatchAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? WILDQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    resolver: { value: input.resolver ?? null, isWritable: false },
    gameConfig: { value: input.gameConfig ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    opponent: { value: input.opponent ?? null, isWritable: true },
    matchAccount: { value: input.matchAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.resolver),
      getAccountMeta(accounts.gameConfig),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.opponent),
      getAccountMeta(accounts.matchAccount),
    ],
    data: getResolveMatchInstructionDataEncoder().encode(
      args as ResolveMatchInstructionDataArgs,
    ),
    programAddress,
  } as ResolveMatchInstruction<
    TProgramAddress,
    TAccountResolver,
    TAccountGameConfig,
    TAccountCreator,
    TAccountOpponent,
    TAccountMatchAccount
  >);
}

export type ParsedResolveMatchInstruction<
  TProgram extends string = typeof WILDQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    resolver: TAccountMetas[0];
    gameConfig: TAccountMetas[1];
    creator: TAccountMetas[2];
    opponent: TAccountMetas[3];
    matchAccount: TAccountMetas[4];
  };
  data: ResolveMatchInstructionData;
};

export function parseResolveMatchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResolveMatchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      resolver: getNextAccount(),
      gameConfig: getNextAccount(),
      creator: getNextAccount(),
      opponent: getNextAccount(),
      matchAccount: getNextAccount(),
    },
    data: getResolveMatchInstructionDataDecoder().decode(instruction.data),
  };
}
